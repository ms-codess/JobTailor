This folder contains reusable application types and ambient type declarations.

Included:
- `resume.ts` — shared FormData type for resume data used across builder, templates, and report.
- `pdfjs-worker.d.ts` — ambient module declaration for `pdfjs-dist` worker import.

